import styled from "styled-components";

export const Container = styled.h1`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);

  font-size: 6.4rem;
  font-weight: 700;
  color: var(--tts-ck-100);
`;
